import React from 'react';
import './notFound.scss';

const NotFound = () => {
    return (
        <div className={'not-found'}>
            <h1 className={'title'}>Такой страницы нет или она еще не создана</h1>
        </div>
    );
};

export default NotFound;
